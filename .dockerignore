# Образы собираются из исходников внутри контейнера: всё, что появляется от локальной сборки,
# в контекст не нужно. Заодно закрыта дыра — устаревший архив из build/libs уезжал в образ
# и выигрывал у шаблона *.jar в Dockerfile.
.git
.github
.gradle
**/build
.idea
.plan
logs

# Секреты и локальное окружение мимо образа.
.env

docs
*.md
!README.md
